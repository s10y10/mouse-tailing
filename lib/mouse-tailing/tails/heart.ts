import type { Position, TailConfig } from '@/types';
import Icon from './icon';

export default class Heart extends Icon {
  constructor(options: TailConfig, position: Position) {
    super(options, position);
  }
}
